# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
POST     /identity/code             controllers.AuthController.generateCode()
POST     /identity/login            controllers.AuthController.generateAccessToken()

PUT      /identity/user             controllers.UserController.updateUser()
